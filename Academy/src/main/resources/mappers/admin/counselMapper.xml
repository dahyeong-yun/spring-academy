<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.academy.mappers.admin.counselMapper">
	<!-- 전체 사용자 리스트 -->
	<select id="userList" resultType="UserVO">
		select user_id, user_login_id, user_password, user_type, user_name, user_email
		from users
	</select>
	
	
</mapper>